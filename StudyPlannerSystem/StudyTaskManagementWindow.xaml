<Window x:Class="StudyPlannerSystem.StudyTaskManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="My Study Tasks" Height="500" Width="750"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="txtHeader" Text="My Study Tasks"
                   FontSize="16" FontWeight="Bold"/>

        <DataGrid Grid.Row="2" x:Name="dgStudyTasks" AutoGenerateColumns="False"
                  IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Task ID" Binding="{Binding TaskId}" Width="60"/>
                <DataGridTextColumn Header="Subject Code" Binding="{Binding Subject.SubjectCode}" Width="100"/>
                <DataGridTextColumn Header="Subject Name" Binding="{Binding Subject.SubjectName}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Day of Week" Binding="{Binding DayOfWeek}" Width="100"/>
                <DataGridCheckBoxColumn Header="Completed" Binding="{Binding IsCompleted}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
