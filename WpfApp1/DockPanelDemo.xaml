<Page x:Class="WpfApp1.DockPanelDemo"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DockPanelDemo">

    <Grid Background="#1E1E2F">
        <DockPanel LastChildFill="True">
            <!-- Top Bar: Navigation & Title -->
            <Border Height="60" Background="#2A2A40" DockPanel.Dock="Top" Padding="15,0">
                <DockPanel LastChildFill="False">
                    <Button x:Name="BtnBack" Content="⬅ Quay lại" Click="BtnBack_Click" DockPanel.Dock="Left" Background="Transparent" Foreground="#A0A0B0" BorderThickness="0" FontSize="14" Cursor="Hand" VerticalAlignment="Center"/>
                    <TextBlock Text="Demo DockPanel" Foreground="White" FontSize="18" FontWeight="Bold" DockPanel.Dock="Left" Margin="20,0,0,0" VerticalAlignment="Center"/>
                </DockPanel>
            </Border>

            <!-- Main DockPanel Demo Area -->
            <Border Margin="20" Background="#252538" CornerRadius="8" Padding="20">
                <DockPanel LastChildFill="True">
                    <!-- Top element in DockPanel -->
                    <Border DockPanel.Dock="Top" Background="#332B1A" Padding="10" Margin="0,0,0,10">
                        <TextBlock Text="Phần trên (Dock = Top)" Foreground="#FFCC00" FontWeight="Bold" HorizontalAlignment="Stretch" TextAlignment="Center"/>
                    </Border>

                    <!-- Bottom element in DockPanel -->
                    <Button Content="Nút ở dưới (Dock = Bottom)" DockPanel.Dock="Bottom" Background="#4A90E2" Foreground="White" Padding="10" Margin="0,10,0,0" FontWeight="Bold" BorderThickness="0"/>

                    <!-- Left element in DockPanel -->
                    <Border Width="150" DockPanel.Dock="Left" Background="#2C2C4E" Margin="0,0,10,0" Padding="10">
                        <StackPanel>
                            <TextBlock Text="Menu trái (Left)" Foreground="White" Margin="0,0,0,10" FontWeight="Bold"/>
                            <TextBlock Text="Nhãn 1:" Foreground="#8FA0B5" Margin="0,5,0,2"/>
                            <TextBox Text="Input 1" Background="#1E1E2F" Foreground="White" BorderBrush="#3A3A55" Padding="5"/>
                            <TextBlock Text="Nhãn 2:" Foreground="#8FA0B5" Margin="0,10,0,2"/>
                            <TextBox Text="Input 2" Background="#1E1E2F" Foreground="White" BorderBrush="#3A3A55" Padding="5"/>
                        </StackPanel>
                    </Border>

                    <!-- Right element in DockPanel -->
                    <Border Width="150" DockPanel.Dock="Right" Background="#2C2C4E" Margin="10,0,0,0" Padding="10">
                        <TextBlock Text="Cột phải (Right) - Dùng hiển thị thông tin mô tả." Foreground="#A0A0B0" TextWrapping="Wrap"/>
                    </Border>

                    <!-- Center/Fill element -->
                    <Grid Background="#1A1A2E">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="200">
                            <TextBlock Text="Phần giữa (LastChildFill = True)" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBox Text="Nhập văn bản ở đây..." Background="#252538" Foreground="White" BorderBrush="#4A90E2" Padding="8"/>
                        </StackPanel>
                    </Grid>
                </DockPanel>
            </Border>
        </DockPanel>
    </Grid>
</Page>
