<Window x:Class="MyStore2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyStore2"
        mc:Ignorable="d"
        Title="MyStore Manager - Premium CRUD Panel" Height="720" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#0f172a" Foreground="#f8fafc"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Fonts -->
        <FontFamily x:Key="MainFont">Segoe UI, Inter, Roboto</FontFamily>

        <!-- Colors -->
        <SolidColorBrush x:Key="BgDark" Color="#0f172a"/>
        <SolidColorBrush x:Key="BgCard" Color="#1e293b"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#334155"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#f8fafc"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#94a3b8"/>
        
        <SolidColorBrush x:Key="PrimaryAccent" Color="#3b82f6"/>
        <SolidColorBrush x:Key="PrimaryAccentHover" Color="#60a5fa"/>
        
        <SolidColorBrush x:Key="SuccessAccent" Color="#10b981"/>
        <SolidColorBrush x:Key="SuccessAccentHover" Color="#34d399"/>
        
        <SolidColorBrush x:Key="DangerAccent" Color="#ef4444"/>
        <SolidColorBrush x:Key="DangerAccentHover" Color="#f87171"/>
        
        <SolidColorBrush x:Key="WarningAccent" Color="#f59e0b"/>
        <SolidColorBrush x:Key="WarningAccentHover" Color="#fbbf24"/>

        <!-- Label Style -->
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,4,0,2"/>
        </Style>

        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0f172a"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryAccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryAccent}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#0f172a"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1e293b"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#3b82f6"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#2563eb"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom Buttons -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryAccent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Opacity" Value="0.75"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" Value="#475569"/>
                                <Setter Property="Foreground" Value="#94a3b8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource SuccessAccent}"/>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource DangerAccent}"/>
        </Style>

        <Style x:Key="WarningButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource WarningAccent}"/>
        </Style>

        <Style x:Key="OutlineButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#1e293b"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#0f172a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid Style -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1e293b"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="#1e293b"/>
            <Setter Property="AlternatingRowBackground" Value="#1a2235"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="RowHeight" Value="38"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="IsReadOnly" Value="true"/>
            <Setter Property="AutoGenerateColumns" Value="false"/>
        </Style>

        <!-- DataGrid Column Header Style -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0f172a"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Height" Value="40"/>
        </Style>

        <!-- DataGrid Cell Style -->
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" SnapsToDevicePixels="true">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="true">
                    <Setter Property="Background" Value="#3b82f6"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <!-- Header Row -->
            <RowDefinition Height="Auto"/>
            <!-- Main Content Row -->
            <RowDefinition Height="*"/>
            <!-- Status Bar Row -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 1. Header Bar -->
        <Border Grid.Row="0" Background="#1e293b" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Modern Store Icon Logo -->
                    <Border CornerRadius="8" Background="{StaticResource PrimaryAccent}" Width="36" Height="36" Margin="0,0,12,0" VerticalAlignment="Center">
                        <TextBlock Text="S" Foreground="White" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="{StaticResource MainFont}"/>
                    </Border>
                    <StackPanel>
                        <TextBlock Text="MyStore Manager" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" FontFamily="{StaticResource MainFont}"/>
                        <TextBlock Text="Product and Category CRUD Management Panel" FontSize="12" Foreground="{StaticResource TextSecondary}" FontFamily="{StaticResource MainFont}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Database status indicator -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse x:Name="elDbStatus" Width="10" Height="10" Fill="{StaticResource WarningAccent}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="lblDbStatus" Text="Database connecting..." Foreground="{StaticResource TextSecondary}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" FontFamily="{StaticResource MainFont}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 2. Main Content Space -->
        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <!-- Left Column: Category CRUD Panel -->
                <ColumnDefinition Width="380"/>
                <!-- Middle Splitter -->
                <ColumnDefinition Width="24"/>
                <!-- Right Column: Product CRUD Panel -->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- A. CATEGORY SIDEBAR PANEL -->
            <Border Grid.Column="0" Background="{StaticResource BgCard}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="180"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <TextBlock Grid.Row="0" Text="Categories Manager" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,12" FontFamily="{StaticResource MainFont}"/>

                    <!-- Category List -->
                    <ListBox Grid.Row="1" x:Name="lstCategories" Background="#0f172a" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" SelectionChanged="lstCategories_SelectionChanged" Margin="0,0,0,16" DisplayMemberPath="CategoryName" ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="12,10"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="true">
                                                    <Setter TargetName="border" Property="Background" Value="#334155"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="true">
                                                    <Setter TargetName="border" Property="Background" Value="#3b82f6"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>

                    <!-- Divider -->
                    <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Margin="0,0,0,12"/>

                    <!-- Category Editor Fields -->
                    <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Category Editor" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,12" FontFamily="{StaticResource MainFont}"/>

                            <!-- Category ID (Read Only) -->
                            <Label Content="Category ID" Target="{Binding ElementName=txtCategoryId}"/>
                            <TextBox x:Name="txtCategoryId" IsReadOnly="true" Background="#1e293b" Foreground="{StaticResource TextSecondary}" Opacity="0.8" Margin="0,0,0,12" Text="[Auto Generated]"/>

                            <!-- Category Name -->
                            <Label Content="Category Name *" Target="{Binding ElementName=txtCategoryName}"/>
                            <TextBox x:Name="txtCategoryName" MaxLength="100" Margin="0,0,0,12" Height="38"/>

                            <!-- Description -->
                            <Label Content="Description" Target="{Binding ElementName=txtCategoryDescription}"/>
                            <TextBox x:Name="txtCategoryDescription" MaxLength="255" Margin="0,0,0,16" Height="70" AcceptsReturn="true" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

                            <!-- Buttons -->
                            <UniformGrid Columns="2" Margin="0,0,0,10">
                                <Button x:Name="btnCategoryAdd" Content="Create" Style="{StaticResource SuccessButton}" Margin="0,0,5,0" Click="btnCategoryAdd_Click"/>
                                <Button x:Name="btnCategoryUpdate" Content="Update" Style="{StaticResource ModernButton}" Margin="5,0,0,0" Click="btnCategoryUpdate_Click"/>
                            </UniformGrid>
                            <UniformGrid Columns="2">
                                <Button x:Name="btnCategoryDelete" Content="Delete" Style="{StaticResource DangerButton}" Margin="0,0,5,0" Click="btnCategoryDelete_Click"/>
                                <Button x:Name="btnCategoryClear" Content="Clear Fields" Style="{StaticResource OutlineButton}" Margin="5,0,0,0" Click="btnCategoryClear_Click"/>
                            </UniformGrid>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Middle Splitter spacing -->
            <Grid Grid.Column="1"/>

            <!-- B. PRODUCT WORKSPACE PANEL -->
            <Border Grid.Column="2" Background="{StaticResource BgCard}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <!-- Title & Search/Filter Controls -->
                        <RowDefinition Height="Auto"/>
                        <!-- Products DataGrid -->
                        <RowDefinition Height="*"/>
                        <!-- Divider -->
                        <RowDefinition Height="Auto"/>
                        <!-- Product Editor Form -->
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header and Filters -->
                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Products List" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" Margin="0,0,24,0" FontFamily="{StaticResource MainFont}"/>
                            
                            <!-- Search -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                                <TextBlock Text="Search:" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="13" FontFamily="{StaticResource MainFont}"/>
                                <TextBox x:Name="txtProductSearch" Width="180" Padding="8,6" FontSize="12" TextChanged="txtProductSearch_TextChanged"/>
                            </StackPanel>

                            <!-- Filter by Category -->
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Category Filter:" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="13" FontFamily="{StaticResource MainFont}"/>
                                <ComboBox x:Name="cmbCategoryFilter" Width="180" Height="32" SelectionChanged="cmbCategoryFilter_SelectionChanged"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Clear Filter Button -->
                        <Button Grid.Column="1" x:Name="btnResetFilters" Content="Reset Filters" Style="{StaticResource OutlineButton}" Padding="10,6" FontSize="12" Click="btnResetFilters_Click"/>
                    </Grid>

                    <!-- Products Grid -->
                    <DataGrid Grid.Row="1" x:Name="dgProducts" SelectionChanged="dgProducts_SelectionChanged" Margin="0,0,0,16">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Product ID" Binding="{Binding ProductId}" Width="100"/>
                            <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="*"/>
                            <DataGridTextColumn Header="Price ($)" Binding="{Binding Price, StringFormat={}{0:N2}}" Width="120"/>
                            <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="100"/>
                            <DataGridTextColumn Header="Category" Binding="{Binding Category.CategoryName}" Width="180"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Divider -->
                    <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Margin="0,0,0,16"/>

                    <!-- Product Editor Form -->
                    <Grid Grid.Row="3">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="Product Editor" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,12" FontFamily="{StaticResource MainFont}"/>
                        
                        <!-- Input Rows -->
                        <Grid Grid.Row="1" Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Product ID (Read Only) -->
                            <StackPanel Grid.Column="0">
                                <Label Content="Product ID" Target="{Binding ElementName=txtProductId}"/>
                                <TextBox x:Name="txtProductId" IsReadOnly="true" Background="#1e293b" Foreground="{StaticResource TextSecondary}" Opacity="0.8" Text="[Auto Generated]"/>
                            </StackPanel>

                            <!-- Product Name -->
                            <StackPanel Grid.Column="2">
                                <Label Content="Product Name *" Target="{Binding ElementName=txtProductName}"/>
                                <TextBox x:Name="txtProductName" MaxLength="100"/>
                            </StackPanel>

                            <!-- Price -->
                            <StackPanel Grid.Column="4">
                                <Label Content="Price ($) *" Target="{Binding ElementName=txtProductPrice}"/>
                                <TextBox x:Name="txtProductPrice"/>
                            </StackPanel>

                            <!-- Quantity -->
                            <StackPanel Grid.Column="6">
                                <Label Content="Quantity *" Target="{Binding ElementName=txtProductQuantity}"/>
                                <TextBox x:Name="txtProductQuantity"/>
                            </StackPanel>
                        </Grid>

                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="24"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Product Category Assignment -->
                            <StackPanel Grid.Column="0">
                                <Label Content="Assign Category *" Target="{Binding ElementName=cmbProductCategory}"/>
                                <ComboBox x:Name="cmbProductCategory" Height="38" DisplayMemberPath="CategoryName" SelectedValuePath="CategoryId"/>
                            </StackPanel>

                            <!-- Action Buttons -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Bottom" Height="38">
                                <Button x:Name="btnProductAdd" Content="Add Product" Style="{StaticResource SuccessButton}" Margin="0,0,8,0" Click="btnProductAdd_Click"/>
                                <Button x:Name="btnProductUpdate" Content="Update Product" Style="{StaticResource ModernButton}" Margin="0,0,8,0" Click="btnProductUpdate_Click"/>
                                <Button x:Name="btnProductDelete" Content="Delete" Style="{StaticResource DangerButton}" Margin="0,0,8,0" Click="btnProductDelete_Click"/>
                                <Button x:Name="btnProductClear" Content="Clear Fields" Style="{StaticResource OutlineButton}" Click="btnProductClear_Click"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- 3. Bottom Status Bar -->
        <Border Grid.Row="2" Background="#0f172a" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="16,8">
            <Grid>
                <TextBlock Text="PRN212 Course Assignment - MyStore2 App Console - Press Clear to prepare new records" Foreground="{StaticResource TextSecondary}" FontSize="12" FontFamily="{StaticResource MainFont}"/>
                <TextBlock HorizontalAlignment="Right" Text="Code First &amp; Programmatic DB Seeding Enabled" Foreground="{StaticResource TextSecondary}" FontSize="12" FontFamily="{StaticResource MainFont}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
